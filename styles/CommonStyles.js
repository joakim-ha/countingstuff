import { StyleSheet } from "react-native";

export const CommonStyles = StyleSheet.create({
  textItem: {
    fontSize: 30,
  },
  textInput: {
    fontSize: 20,
    flexShrink: 1,
    paddingEnd: 0,
    color: "grey",
  },
});
